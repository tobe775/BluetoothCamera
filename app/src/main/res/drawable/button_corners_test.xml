<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ボタンが押されてない時 -->
    <item android:state_pressed="false">
        <!-- ボタンの形状 -->
        <shape android:shape="rectangle">
            <!-- ボタンの色 -->
            <solid android:color="#ffdf4b4d"/>
            <!-- ボタンの枠線 -->
            <stroke android:width="0dp" android:color="#ffdf4b4d"/>
            <!-- ボタンの角の半径 -->
            <corners android:radius="2dp"/>
            <!-- ボタン内の余白 -->
            <padding android:top="8dp" android:left="16dp" android:bottom="8dp" android:right="16dp"/>
        </shape>
    </item>
    <!-- ボタンが押された時 -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#ffc14143"/>
            <stroke android:width="0dp" android:color="#ffc14143"/>
            <corners android:radius="2dp"/>
            <padding android:top="8dp" android:left="16dp" android:bottom="8dp" android:right="16dp"/>
        </shape>
    </item>
</selector>
